import { View } from "react-native";

// AdMob Banner is temporarily disabled to ensure app stability.
// Real ads will be re-enabled in a future update.
export default function BannerAdView() {
  return <View />;
}
